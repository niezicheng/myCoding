分支语句：
	if else
	if else if else
	switch()
		case 1:
			...
			break;
		default:
			...
			break;
循环语句：
	for(;;){}
	while(){}
	do{}while()

	break;
	continue;
	label语句可以给for循环起别名
		break name;
		continue name;

对象
	两种创建对象的方式

函数

